# foodstyles-todo-app-server

NodeJS server for simple todo app with authentication

Add .env file with next values to test

`DATABASE` 

`DATABASE_USER`

`DATABASE_PASSWORD`

`JWT_SECRET` this can be anything